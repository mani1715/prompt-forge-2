# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to Git (it's in .gitignore)

# ============================================================================
# BACKEND API URL (REQUIRED)
# ============================================================================
# Backend API endpoint
# Local development: /api (uses proxy)
# Production: Full backend URL like https://api.yourdomain.com/api
REACT_APP_BACKEND_URL=/api

# ============================================================================
# WEBPACK DEV SERVER (DEVELOPMENT ONLY)
# ============================================================================
# WebSocket port for hot reload (development only)
WDS_SOCKET_PORT=443

# ============================================================================
# OPTIONAL FEATURES
# ============================================================================
# Enable/disable health check endpoint
ENABLE_HEALTH_CHECK=false

# Use webpack proxy for API calls (development)
USE_WEBPACK_PROXY=true

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# LOCAL DEVELOPMENT:
# - Keep REACT_APP_BACKEND_URL=/api
# - Backend runs on localhost:8001
# - Frontend runs on localhost:3000
#
# PRODUCTION DEPLOYMENT:
# - Change REACT_APP_BACKEND_URL to your backend URL
# - Example: REACT_APP_BACKEND_URL=https://your-backend.onrender.com/api
# - Or if using same domain: REACT_APP_BACKEND_URL=/api
#
# IMPORTANT:
# - Never commit .env with production values to Git
# - Set environment variables in your hosting platform (Vercel, Netlify, etc.)
# ============================================================================